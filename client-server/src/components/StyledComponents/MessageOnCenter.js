import styled from 'styled-components';

const MessageOnCenter = styled.h1`
  position: absolute;
  color: white;
  left: 50%;
  top: 40%;
  transform: translate(-50%, -50%);
`;

export default MessageOnCenter;
